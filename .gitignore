# === OS ===
.DS_Store
Thumbs.db

# === IDE (Antigravity) ===
.antigravity/
.idea/

# === Dependencies ===
node_modules

# === Logs ===
*.log

# === Environment ===
# Arquivos com valores reais — NUNCA comitar
.env
.env.local
.env.*.local
.env.production
.env.staging

# .env.example era o template antigo — substituído por .env.schema (varlock)
.env.example

# .env.schema é a fonte da verdade (decorators, tipos, defaults sem segredos) — COMITAR
!.env.schema

# Tipos gerados pelo varlock a partir do .env.schema — podem ser comitados ou não
# (dependendo do seu workflow — comitar evita regenerar no CI)
env.d.ts

# === Turborepo ===
.turbo

# === Bun ===
bun.lockb.bak

# === Vite ===
.vite
dist
dist-ssr

# === TanStack Start / Nitro ===
.tanstack
.output
.nitro

# === Astro Starlight ===
.astro

# === TypeScript (tsgo) ===
*.tsbuildinfo
tsgo-cache/

# === Build / Output ===
.output
out
coverage

# === Docker ===
.docker/

# === Fly.io ===
.fly/

# === VSCode (caso alguém do time use) ===
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
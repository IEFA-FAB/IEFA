# Assets estáticos — rumaer

Arquivos aqui são servidos na raiz do site pelo Vite (`/<arquivo>`).

## Imagens esperadas

- `uniforme-historico.png` — foto do uniforme histórico (cadete/oficial, túnica
  azul-ferrete + quepe branco + espadim). Usada como hero na landing
  (`src/routes/_public/index.tsx`, coluna direita do HERO).
  - Recomendado: PNG com fundo transparente, figura de corpo inteiro em pé,
    altura ≥ 800px. O layout escala por `max-h` e mantém o aspecto.

/**
 * Tipos de domínio para o MCP server do sisub.
 * Re-exportados de @iefa/sisub-domain (fonte canônica).
 */

export type { AppModule, PermissionScope, UserContext, UserPermission } from "@iefa/sisub-domain/types"

/**
 * Re-exporta os tipos PBAC do pacote compartilhado @iefa/pbac.
 * A definição canônica vive em packages/pbac/src/types.ts.
 */
export type { AppModule, PermissionScope, UserPermission } from "@iefa/pbac"
